//! Name resolution and desugaring over `rphp_ast::v2` (ADR-026): the HIR is a
//! newtype over the same AST vocabulary whose invariant is "only the canonical
//! subset appears". Stub — filled in by workstream F4.
#![forbid(unsafe_code)]
