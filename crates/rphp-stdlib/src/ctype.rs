//! `ctype` extension — character-class predicates. (Stub: filled by the Tier-A
//! burn-down.)
use crate::NativeFn;

/// This extension's registry contribution (see `lib.rs`).
pub(crate) static FUNCTIONS: &[NativeFn] = &[];
