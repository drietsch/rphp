//! `hash` extension — message digests (md5, sha1, crc32, hash()). (Stub: filled
//! by the Tier-A burn-down; backed by pure-Rust digest crates.)
use crate::NativeFn;

/// This extension's registry contribution (see `lib.rs`).
pub(crate) static FUNCTIONS: &[NativeFn] = &[];
