//! `json` extension — `json_encode` / `json_decode`. (Stub: filled by the
//! Tier-A burn-down. Decoding targets the assoc-array form; the default
//! object form waits on the object value type.)
use crate::NativeFn;

/// This extension's registry contribution (see `lib.rs`).
pub(crate) static FUNCTIONS: &[NativeFn] = &[];
