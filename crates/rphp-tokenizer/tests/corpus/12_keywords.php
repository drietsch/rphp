<?php
abstract and array as break callable case catch class clone const continue declare default do echo else elseif empty enddeclare endfor endforeach endif endswitch endwhile enum eval exit extends final finally fn for foreach function global goto if implements include include_once instanceof insteadof interface isset list match namespace new or print private protected public readonly require require_once return static switch throw trait try unset use var while xor yield die
ABSTRACT And ARRAY As Break CALLABLE Case Catch CLASS Clone CONST Continue Declare Default Do ECHO Else ElseIf Empty EndDeclare EndFor EndForeach EndIf EndSwitch EndWhile Enum Eval Exit Extends Final Finally Fn For Foreach Function Global Goto If Implements Include Include_Once InstanceOf InsteadOf Interface Isset List Match Namespace New Or Print Private Protected Public ReadOnly Require Require_Once Return Static Switch Throw Trait Try Unset Use Var While Xor Yield Die
__CLASS__ __TRAIT__ __FUNCTION__ __PROPERTY__ __METHOD__ __LINE__ __FILE__ __DIR__ __NAMESPACE__ __class__ __Line__ __halt_compiler_not __HALT_COMPILERx halt_compiler
yield from $g; yield  from$g; yield
from $g; yield /* c */ from $g; yield // c
from $g; yield # c
from $g; yield fromage; yield from_; yieldfrom; yield; yield FROM $g; YIELD From $g; yield	from $g; yield from; yield from1;
enum Foo {} enum Bar: int {} enum extends X {} enum implements Y {} enum extendsX {} enum implementsY {} enum(1); enum; enum
Baz {} enum /* c */ Qux {} enum // c
Quux {} enum # c
Corge {} enum 1; enum $x; enum{} enum\Foo; Enum Grault {} ENUM EXTENDS; enum _x {} enum é {} enum implements
public(set) protected(set) private(set) PUBLIC(SET) Public(Set) public (set) public( set) public(set)x public(setx) public(set
$o->class; $o->list; $o?->array; $o->enum; $o->yield; $o -> function; $o->fn(); $o::class; Foo::class; Foo::list; Foo::CLASS; static::new; self::parent; Foo::enum; Foo::yield;
exit; die(); exit(1); __halt_compilerx; exitx; dies; fnx; x_fn; _fn;
