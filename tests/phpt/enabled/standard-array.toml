# php-src slice scored by `cargo xtask phpt --ext standard-array`; schema in tests/phpt/README.md
dir = "ext/standard/tests/array"
enabled = true
baseline = 0
notes = "array_* functions, sorting, range/compact/extract; some tests print memory-limit fatals"
